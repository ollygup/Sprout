# 86 — Rail order: quick surfaces first with cluster dividers

**What to build:** The navigation rail leads with the three daily surfaces — Quick Launch, Quick Actions, Quick Clips — then the setup cluster (Products, Presets, Plan), then reference (History, Logs, Settings), with thin hairline dividers separating the three clusters. No labels on dividers; item sizing, active/hover treatment, and keyboard behavior unchanged.

**Blocked by:** None — can start immediately.

**Status:** done

- [x] Rail order reads quick trio → setup trio → reference trio
- [x] Dividers visually separate clusters without adding headings or changing item metrics
- [x] Focus order and current-page indication behave exactly as before
- [x] Type-check clean

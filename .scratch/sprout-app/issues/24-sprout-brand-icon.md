# 24 — Sprout brand icon

**What to build:** A proper app identity: a sprout glyph (stem + two leaves) in botanical green on a neutral field, used as the Tauri application icon (regenerated across all sizes), the window icon, the favicon, and the nav wordmark. theme-color updated to match the new identity per mode.

**Blocked by:** 11 — App shell and design foundation

**Status:** done

- [x] Sprout glyph designed (stem + two leaves, botanical green on neutral) in the seed-catalog identity
- [x] Tauri icons regenerated from the glyph; window icon and favicon updated; nav wordmark uses it
- [x] Icon reads at 16px (favicon/nav) and at large sizes; dark and light variants both legible
- [x] `npm run check` 0 errors, `cargo test` green, `cargo check` clean, `npm run build` ok
# Batch batch-149-168-20260909 — parallel 149 + 168 (167 queued)

Coordinator: single session in `C:\Sprout`. Core `-Down` ran 2026-09-09 as first action (0 copied, snapshot 455 files). ADR index read; relevant ADRs read in full: 0028, 0029, 0030, 0031, 0032. Source baseline: `.sync-state.json` taken `2026-09-09T05:23:57-07:00`, copied to `C:\Users\admin\AppData\Local\Temp\opencode\batch-149-168-20260909\baseline-hashes.json` (immutable, outside synced tree).

167 is intentionally NOT in this parallel wave: it edits helper copy in the same pages/dialogs both workers touch. It runs sequentially after integration to avoid a three-way copy-merge.

## Ticket table

| Ticket | Dependencies | Baseline | Workspace | Claimed paths / owners | Shared contract / integration owner | Actual changes | Validation assignment / results | State |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 149 AI scoped discovery | 148 (implemented awaiting publish; `ai_generate_draft` at `src-tauri/src/lib.rs:1771`) — behavior-ready | sync 2026-09-09T05:23:57, 455 files | `...\batch-149-168-20260909\worker-a-149` | NEW `ai_discovery.rs`; EDIT `ai_assist.rs`/`lib.rs`/`db.rs`/`api.ts`/`types.ts`/`QuickActionFormDialog.svelte` (regions only) | Opaque ref contract, coordinator merges api/types | first dispatch aborted before start; retried compact and returned | Coordinator: `cargo test ai_` 43 pass; full `cargo test` 530 pass/3 ignored; `cargo check` clean; `npm check` 0 errors; `npm test` 178 pass (after 1-line aiReady test-gate fix). Coordinator repairs: stale-before-link order in `revalidate_file`, 3 warning allows/scopings, test gate fix. | validated |
| 168 dock visibility + 169 Start coupling | none (builds on 159 `show_in_dock`); 169 consumed here for honest ship (filter without matching Start would lie) | same baseline | `C:\Users\admin\AppData\Local\Temp\opencode\batch-149-168-20260909\worker-b-168` | NEW `src/lib/dockVisibility.ts` (+ test) and `src/lib/components/DockVisibilityFilter.svelte`; edit `src/routes/+page.svelte`, `src/routes/quick-actions/+page.svelte`, `src/routes/clips/+page.svelte` (indicator + filter + reorder-guard regions only); edit `src/lib/api.ts` (`startQuickLaunch(ids?)` only); edit `src-tauri/src/lib.rs` (`start_quick_launch` optional-ID selection only, through existing `launch_entries` batch once); reuse `ContextMenu.svelte`, `PageHeader.svelte`, `Icon` eye-off. No storage migration, no new hidden-state flag. | Matching-collection contract owned by 168: `matching = full.filter(text AND visibility)`, saved order preserved, group collapse/scroll never change set; `isFilterActive = text≠"" OR vis≠All`; Quick Launch Start reads IDs/count from it at activation. `api.ts` overlap merged by coordinator. | pending return → returned clean. Coordinator merged `api.ts` (B's Start block + A's discovery block) and `lib.rs` (B's selection + A's commands) in staging; no silent overwrites. | Coordinator: `npm check` 0 errors; `npm test -- dockVisibility` 12 pass; full `npm test` 178 pass; `cargo test launch::` 46 pass/1 ignored; full `cargo test` 530 pass/3 ignored; gate pass (62 refs). Open: keyboard/Escape/focus + light/dark + narrow manual pass (168 AC6) needs human eyes. | validated (AC6 manual pending) |
| 167 field-guidance cleanup (sequential, post-unit-1) | none in round; preserve 157 submit grammar | unit-1-published tree | `C:\Sprout` (coordinator, no worker) | FormDialogs, GroupNameDialog (none), companion/+page, settings/+page, +page installed-app hint, PresetFormDialog, aiDraftDialog.test.ts | 168/149 new copy preserved verbatim | all 6 keyboard hints removed; tutorials consolidated; dock-mode stale copy corrected; 3 aria-labels added; 2 dead CSS selectors pruned; per-field audit appended to ticket | `npm check` 0/0; `npm test` 178 pass; gate pass; no Rust touched. Open: runtime light/dark + keyboard-only pass (AC9) with 168 AC6. | validated (AC9 manual pending) |

Staging: `C:\Users\admin\AppData\Local\Temp\opencode\batch-149-168-20260909\staging` (reconciliation only; checks run in `C:\Sprout` per toolchain single heavy slot).

## Overlap plan (before dispatch)

- `src/lib/api.ts`, `src/lib/types.ts`: both workers append different exported functions/types. Coordinator merges textually (disjoint symbol blocks) in staging via three-way baseline/worker/staging compare. No silent last-writer-wins.
- `QuickActionFormDialog.svelte`: only Worker A writes (AI region). Worker B MUST NOT touch it. 167 later owns helper copy there; A must preserve all existing helper/keyboard copy verbatim except its own new find/bind block.
- Collection pages: only Worker B writes. Worker A MUST NOT touch them.
- Shared owners/tokens/constants/version: no changes allowed by either worker without coordinator approval.

## Integration order

1. Collect returns, freeze artifacts. 2. Reconcile B in staging, then A (A's dialog region is independent of B's pages). 3. Apply candidate to `C:\Sprout` path-by-path with pre-apply recovery snapshot outside tree. 4. Validate combination in `C:\Sprout` (toolchain checks + ticket tests + ownership gate). 5. Mark ACs only for applied+validated tickets. 6. Publish per completed unit with `-Up` twice (expect 0 copied). 7. Then run 167 sequentially.

## Conflict ledger

- `src/lib/api.ts` (149A + 168B): disjoint symbol blocks — B's `startQuickLaunch(ids?)` (lines 238–251 region) + A's appended discovery wrappers. Coordinator staged B's file, appended A's block verbatim. Both preserved. Validated by `npm check` + tests.
- `src-tauri/src/lib.rs` (149A + 168B): B's `start_quick_launch` selection + A's `mod ai_discovery`, `grants:none()`, 8 discovery commands, 8 registrations. Coordinator staged B's file, applied A's 4 edits. Both preserved. Validated by `cargo check`/`cargo test`/gate.
- Coordinator repairs on A's return (in `C:\Sprout` + mirrored to staging): existence-before-reparse order in `revalidate_file` (fixes stale misreport; failing test now passes), `#[cfg(test)]` scoping of 2 imports + 2 `#[allow(dead_code)]` (zero-warning `cargo check`), `aiReady:true` in `aiDraftDialog.test.ts` render (gate was already the accepted design; test predates it).
- `src/lib/types.ts`, collection pages, dialog, `db.rs`, `ai_assist.rs`, `launch.rs`: single-writer, copied verbatim.

# Batch batch-146-147-20260908 — tickets 146 + 147

Coordinator: single-agent session (no spawned workers). Reason: two dependency-ready tickets with disjoint paths; sequential single-writer implementation avoids overlapping-writer hazard while satisfying parallel-ticket rules (one coordinator syncs/publishes, no second sync session, no shared-tree concurrent writers).

Baseline: immutable hashes at `C:\Users\admin\AppData\Local\Temp\opencode\batch-146-147-20260908\baseline-hashes.txt` (SHA256, 2026-09-08, post `-Down` snapshot 434 files).
Baseline source for both tickets: same `-Down` snapshot.

## Ticket table

| Ticket | Dependencies | Baseline | Workspace | Claimed paths / owners | Shared contract / integration owner | Actual changes | Validation assignment / results | State |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 146 — AI model/runtime/provider qualification | None — ready | batch-146-147-20260908 | C:\Sprout (single writer, sequential) | `docs/research/0018-ai-qualification-support-report.md` (new), `src-tauri/resources/ai-skills/shared-rules.md` (new), `src-tauri/resources/ai-skills/create-quick-action.md` (new), `src-tauri/resources/ai-skills/diagnose-quick-action.md` (new), `src-tauri/resources/ai-skills/NOTICES.md` (new), `src-tauri/resources/ai-model-recommendations.json` (new), `src-tauri/tests/ai-eval-fixtures.json` (new), `src-tauri/tauri.conf.json` (bundle.resources), `.scratch/sprout-app/issues/146-ai-model-runtime-provider-qualification.md` (AC marks) — no Rust/JS owners | Contracts pinned by 146 for 148+: destructive-refusal corpus shape, skill file paths, recommendation JSON shape, eval-fixture shape, existing-local/cloud protocol shape. 146 owns these new paths; 147 must not touch them. | Delivered as planned; result hashes in `result-hashes.txt` (same batch dir as baseline) | JSON parse ok (node); ownership gate pass (no new Windows ops, 62 refs); full `cargo test --lib` 485 passed 0 failed; `npm run check` 0 errors | validated |
| 147 — PowerShell/CMD Quick Actions + backup v2 | None — ready | batch-146-147-20260908 | C:\Sprout (single writer, sequential) | `src-tauri/src/quick_actions.rs` (QuickActionShell owner), `src-tauri/src/windows_execution/process.rs` + `windows_execution.rs` (shell argv owner, ADR-0029), `src-tauri/src/backup.rs` (envelope v2 owner), `src-tauri/src/db.rs` (shell migration), `src-tauri/src/lib.rs` (test_quick_action shell param), `src/lib/types.ts` + `src/lib/api.ts` (QuickActionShell), `src/lib/components/QuickActionFormDialog.svelte` + `QuickActionDetailsDialog.svelte` (shell UI), `src/routes/quick-actions/+page.svelte` (shell badge), `.scratch/sprout-app/issues/147-quick-action-shells-and-backup-compatibility.md` (AC marks) — 146 paths excluded | Shared contract: `QuickActionShell = powershell\|cmd` (serde lowercase), legacy default powershell, unknown fails; backup v2 envelope (`BACKUP_VERSION=2`), v1 legacy read; collision identity = shell+command+cwd. Owned by 147; 146 does not consume it yet (148 will). | Delivered as planned; result hashes in `result-hashes.txt` | `cargo test --lib` 485 passed 0 failed (incl. 40 quick_actions, 25 backup, 36 db, 11 groups, 7 windows_execution); `cargo check` clean; `npm run check` 0 errors; ownership gate pass | validated |

Integration order: 147 code first (establishes shell contract 146 references), then 146 docs/fixtures (reference shell contract, no code overlap). No file overlap between tickets except batch record + issue files (distinct files per ticket). No shared Rust/JS owner overlap.

Candidate IDs: single local candidate `cand-147-then-146` applied directly in C:\Sprout (single-writer sequential, no staging copy needed — no concurrent writers to reconcile). Pre-apply recovery: baseline hashes above + snapshot via sync script; any restore uses baseline file contents, never inverse-guess edits.

Resource policy: one heavy slot in C:\Sprout for `cargo test/check`, `npm run check`, ownership gate. No isolated build copies, no extra installs. No exceptions.

## Conflict ledger

none — disjoint paths by design (146: new docs/resources/tests JSON; 147: existing quick-action/backup/execution/frontend). Three-way comparison not needed (single writer, sequential).

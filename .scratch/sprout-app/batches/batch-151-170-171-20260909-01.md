# Batch 151 / 170 / 171 — 2026-09-09

**Batch ID:** `batch-151-170-171-20260909-01`

**Coordinator working copy:** `C:\Sprout`

**Immutable baseline:** `C:\Users\admin\AppData\Local\Temp\sprout-batches\batch-151-170-171-20260909-01\baseline`

**Baseline manifest:** `C:\Users\admin\AppData\Local\Temp\sprout-batches\batch-151-170-171-20260909-01\baseline-hashes.tsv`

**Manifest SHA-256:** `aec6a42745e035e3b52dc8e77cf9917686b989527097b7a1f71bdb619478ee51`

The baseline contains 462 source/document files and excludes `.git`,
`.sync-state.json`, `.codegraph`, `node_modules`, `target`, `build`, and
`.svelte-kit`. Planned new-path absence is recorded in
`baseline-absent.txt` beside the manifest.

## Plan and state

| Ticket | Dependencies | Baseline / workspace | Claimed paths and owner symbols | Shared contract / integration owner | Validation assignment | Actual changes | State |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 151 | 146 and 148 implementations are present in this baseline; their tracker status still says awaiting validation/publish, so the combined checks must validate the inherited provider and checked-draft flow too | baseline manifest above / stopped worker `...\ticket-151`; frozen partial `...\returns\ticket-151-partial-usage-limit`; coordinator continuation `...\repair-151-r1` | `src-tauri/src/ai_assist.rs` AI orchestration/provider seam; optional new `ai_managed.rs`; `windows_execution/{process.rs,mod owner}` for native process mechanics; `lib.rs` commands and actual-exit hook; `settings.rs`; bundled recommendation/resources and Tauri packaging; frontend Settings, types, API, Quick Action form, Quick Actions readiness predicate, and focused tests; ticket 151 | Reuse `DraftProvider` + `request_draft`; one qualified runtime integration for compatible models; managed process ownership remains distinct from foreign existing-local services; Windows launch/kill mechanics stay in `windows_execution`. Ticket 151 owns these paths. | Worker stopped on account usage limit after 10 changed paths; no return checks. Coordinator is reviewing/completing the frozen partial in a new repair copy, then will run Rust/frontend/fake-lifecycle checks in `C:\Sprout`. | Partial managed module/wiring recovered; qualification/manual claims remain gated. | running |
| 170 | none; existing active-site, identity, zoom, and profile lifecycle are baseline contracts | baseline manifest above / stopped worker `...\ticket-170`; frozen partial `...\returns\ticket-170-partial-usage-limit`; coordinator return `...\returns\ticket-170-r1` | `src/routes/quick-launch-window/+page.svelte` Companion toolbar and child lifecycle; `src/lib/companion.ts`; focused tests; `src/routes/companion/+page.svelte` + manager test for zoom preservation | Ticket 170 is sole production owner for picker/activation. Use existing `setCompanionUrl`; one live child; selecting active is a no-op; switch opens saved URL with saved identity/zoom and existing profile. No navigation interception. | Coordinator completed the stopped worker's return, reviewed it against the UI rules, and applied it in `candidate-companion-170-171-r1`; frontend tests/check/build plus native dock smoke pending. | Five paths: selector/queue/helper, focused picker test, manager zoom-preservation fix/test. Exact hashes in frozen return. | awaiting-validation |
| 171 | exact reporter reproduction remains missing; ticket explicitly permits retaining that limitation | baseline manifest above / `...\ticket-171`; frozen return `...\returns\ticket-171-r2` | `docs/research/0012-companion-webview-feasibility.md`; ticket 171; bounded new controlled navigation fixtures/repro tooling only. No production `src/` or `src-tauri/src/` changes | Research/fixture owner only. Observe 170's accepted switch lifecycle but do not modify it. Any repair is a new bounded ticket plus ADR/policy amendment if evidence justifies it. | Worker: `node --check` on both `.mjs` files passed; deterministic verifier passed `RESULT 8/8 controlled contracts passed`. Coordinator native matrix remains pending on trusted HTTPS + isolated profile prerequisites. | Research and ticket updated; added fixture server/README/verifier. 5/6 ACs checked; native matrix remains open. Exact hashes are in the frozen return and worker handoff. | awaiting-validation |

## Integration order

1. Reconcile 170 first because it owns the only production Companion change.
2. Reconcile 171's fixtures/research against the 170 candidate, then run the
   native behavior matrix without turning observations into an unapproved fix.
3. Reconcile 151 independently; its inherited 146/148 provider flow is part of
   the same validation candidate. If repeated heavy checks justify grouping,
   151 may share the final frontend/Rust pass with 170/171 after focused tests.
4. Apply one reviewed candidate to `C:\Sprout`, preserve recovery copies and
   hashes, validate the combined tree, update each ticket's ACs only for
   evidence actually obtained, run the ownership gate, publish with guarded
   `-Up`, then verify a second `-Up` reports `0 copied`.

## Shared contracts

- The `DraftProvider` / `request_draft` interface remains the deep authoring
  seam. Managed installation and owned-runtime lifecycle add an adapter and
  lifecycle implementation; callers do not get a second generation pipeline.
- `windows_execution` remains the sole module owning Windows process launch,
  hidden-window mechanics, tree termination, and related native invocation.
- Companion activation continues through `setCompanionUrl`; ticket 170 owns
  production picker/lifecycle code, while 171 is prohibited from speculative
  routing, popup, bridge, isolation, reload, or tab changes.
- Saved-site authoring stays in the main app. The dock exposes frequent
  selection only (research 0004 rules 2–3); Open externally is a distinct
  action. Existing design tokens and shared button/menu primitives remain the
  visual interface.

## Candidate and recovery ledger

The final integrated source candidate is `candidate-151-170-171-r6`. It
contains frozen returns `ticket-151-r2`, `ticket-170-r4`, and
`ticket-171-r2`. Earlier candidate snapshots remain immutable for provenance.
Its 467-file source manifest, excluding this self-referential batch ledger,
has SHA-256
`cdfc81b57ced352241e101a6fcdd63547ca81626d51c4ecddedcf41d63bced7f`.
The original Companion recovery is
`recovery/candidate-companion-170-171-r1`; the ticket 151 pre-apply recovery is
`recovery/before-ticket-151-r3`. New paths are absent from their corresponding
recoveries. Artifacts live under the batch root's `candidates/`, `returns/`,
and `recovery/` directories.

## Baseline prerequisite validation

- `cargo test ai_assist` from `C:\Sprout\src-tauri`: passed 21 tests
  (512 filtered out) on the immutable source baseline; compilation emitted 62
  pre-existing `tempfile::TempDir::into_path` deprecation warnings.
- `npm.cmd test -- src/lib/aiDraftDialog.test.ts src/lib/settingsSearch.test.ts`
  from `C:\Sprout`: passed 2 files / 25 tests.

These results validate the inherited 146/148 provider, refusal, checked-draft,
and Settings search seams before ticket 151 integration. They do not validate
ticket 151's managed-runtime changes.

## Conflict ledger

None at dispatch. Planned conceptual overlap between 170 and 171 is resolved
by assigning all production Companion code to 170 and all diagnosis artifacts
to 171.

- `ticket-171-r1` fixture reflected two query values into controlled HTML.
  Coordinator review requested escaping plus an injection assertion;
  `ticket-171-r2` replaced it. No production/shared-contract conflict.
- Baseline preview-only Back/Forward state has no native-WebView callback.
  Ticket 171 recorded the distinction; ticket 170 must not promote it into a
  native history contract.

## Final integration record — 2026-09-10

- Ticket 151: generic managed setup/runtime integration is implemented and
  validated fail-closed. Seven of ten ACs are checked. The bundled catalog is
  still non-installable because research 0018 has not qualified a model/runtime
  pair; exact catalog evidence, real managed end-to-end generation, and the
  installed-build/manual hardware smoke remain open.
- Ticket 170: implementation and automated validation are complete. Six of
  eight ACs are checked. The real native dock matrix/smoke across edges, modes,
  DPI, focus, and auto-hide remains open.
- Ticket 171: controlled fixture/source investigation remains complete at five
  of six ACs. The trusted-HTTPS isolated-profile native matrix and exact
  reporter reproduction remain open.

Combined validation:

- `npm.cmd run check`: 0 errors, 0 warnings.
- `npm.cmd test`: 15 files, 185 tests passed.
- `npm.cmd run build`: passed.
- `cargo test ai_managed`: 8 tests passed.
- Full `cargo test`: 537 passed, 3 ignored; one registry round-trip failed only
  under sandbox registry denial and passed when rerun with registry access.
- Ticket 171 verifier: `RESULT 8/8 controlled contracts passed`; both fixture
  scripts pass `node --check`.
- `cargo fmt --check` unavailable because `rustfmt` is not installed in the
  machine's only Rust toolchain.

# Batch batch-174-175-177-178-179-20260911 — parallel 174 + 175 + 177 + 178 + 179

Coordinator: single session in `C:\Sprout`. Core `-Down` ran 2026-09-11 as first action (0 copied, snapshot 470 files). ADR index read; relevant ADRs read in full: 0014, 0017, 0022, 0026, 0029. Source baseline: `.sync-state.json` taken `2026-09-11T06:44:55.4532242-07:00`, copied to `C:\Users\admin\AppData\Local\Temp\opencode\batch-174-175-177-178-179-20260911\baseline-hashes.json` (immutable, outside synced tree).

Spec owner: 173. All five tickets are wave-1 dependency-ready per spec table (174 alone, 175 contract-owner, 177 independent, 178 independent, 179 contract-owner). 176/180 are wave-2 and NOT in this batch; 175/179 settle contracts for them. Backup overlap (175/178/179) is coordinated, not sequenced.

## Ticket table

| Ticket | Dependencies | Baseline | Workspace | Claimed paths / owners | Shared contract / integration owner | Actual changes | Validation assignment / results | State |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 174 Logs order (coordinator-owned) | None — frontend-only, preserves 83 | sync 2026-09-11T06:44:55, 470 files | `...\batch-174-175-177-178-179-20260911\worker-174-coord` | EDIT `src/routes/logs/+page.svelte` render order only (`launch` → `actions` → `runs`); no other file | No contract change; pure reorder; coordinator merges trivially | Order-only diff verified; `npm.cmd run check` 0/0; `npm.cmd test` 212 pass (batch tree) | validated |
| 175 Pre-action backend | None — contract owner for 176. Assumes 147 shell semantics unchanged | same baseline | `...\batch-174-175-177-178-179-20260911\worker-175` | EDIT `src-tauri/src/quick_actions.rs` (pre_check/pre_fix shape, validate, run chain) + `db.rs` migrate + `lib.rs` commands + `backup.rs` tests; `windows_execution/` untouched (full reuse) | Pinned: stored `pre_check`/`pre_fix` Option<String> trimmed empty→None, fix-without-check refused; check-first chain reusing Test core with dedicated 10s box (existing TEST_TIMEOUT is 20s — left alone); warn payload (trimmed output + has_fix + durations); same-log headers pre-check/pre-fix/action; backup additive same document, identity shell+command+cwd (already implemented — regression-tested); ADR-0029 single owner | `cargo test` 585 pass (incl. 12 pre-action tests); `cargo check` 0/0; `npm.cmd run check` 0/0; gate pass. Manual Tauri matrix pending | validated (manual pending) |
| 177 Companion Back/Forward | None — independent. Assumes 170 lifecycle; 171 investigation-only gates nothing | same baseline | `...\batch-174-175-177-178-179-20260911\worker-177` | EDIT `src/routes/quick-launch-window/+page.svelte` Companion toolbar/lifetime + trail + reload-saved; NEW `trailApplyVisit` in `src/lib/companion.ts`; EDIT `src/lib/companionPane.test.ts` (+12 tests); EDIT `docs/adr/0022-companion-single-isolated-site.md` (append Amendment only) | Pinned: Back/Forward sole history-chrome exception; reload/retry = saved address; switch replaces at saved address; no tabs/omnibox/bridge/policy; UI-heavy rule binding (0004:1/4/5, 0006, 0008-n/a) | `npm.cmd run check` 0/0; `npm.cmd test` 212 pass; gate pass. AC1 PARTIAL (switch-trail only — native in-pane traversal needs follow-up Rust seam); manual dock matrix pending | returned→integrated, AC1 open |
| 178 Image Clips | None — independent. Extends 78/79, Groups 89–91 isolated | same baseline | `...\batch-174-175-177-178-179-20260911\worker-178` | EDIT `src-tauri/src/clips.rs` + `db.rs` (`clip_images` FK CASCADE) + `lib.rs` (4 clip-image commands + copy_clip guard) + `backup.rs` (merge split) + `groups.rs` (1-line test literal); EDIT clips page + quick-launch-window/tab Clips regions + dock; EDIT `src/lib/api.ts`/`types.ts` clip-image symbols | Pinned: PNG+JPEG normalized, 5MB cap, one image per Clip v1; backup bytes + image identity (FNV-1a bytes hash, name display-only); text-clip behavior untouched; UI-heavy rule binding (0004/0005/0006 cited) | `cargo test` 585 pass (incl. 10 clip + 3 backup-image tests); `cargo check` 0/0; `npm.cmd run check` 0/0 after coordinator import repair; gate pass. AC2 manual dock/a11y matrix pending | validated (AC2 manual pending) |
| 179 Action files backend | None — contract owner for 180. Assumes 175 run/log chain + 147 shell semantics | same baseline | `...\batch-174-175-177-178-179-20260911\worker-179` | EDIT `src-tauri/src/quick_actions.rs` files interface + `db.rs` (`quick_action_files` FK CASCADE) + NEW `windows_execution/files.rs` + `windows_execution.rs` re-export + `backup.rs`/zip export + `lib.rs` files commands | Pinned: basename-only CI-unique, 5MB/file 20MB/action caps before write; stage per-run temp dir, expand `<FilesDir>` once per run shell-quoted by owner, no-placeholder = inert; zip only when files exist else same JSON as 160; identity = extended action identity + filename set | `cargo test` 585 pass (incl. files/staging/quote/zip/merge tests); `cargo check` 0/0 after coordinator borrow + re-export repairs; `npm.cmd run check` 0/0; gate pass. Manual quoting matrix pending | validated (manual pending) |

Staging: `C:\Users\admin\AppData\Local\Temp\opencode\batch-174-175-177-178-179-20260911\staging` (reconciliation only; checks run in `C:\Sprout` per toolchain single heavy slot).

## Overlap plan (before dispatch)

- `src-tauri/src/backup.rs` + `db.rs` + `lib.rs` (175 + 178 + 179): three writers append different additive regions to the same `sprout-backup` document (ADR-0014, no second format; empty arrays never absent keys). Coordinator merges textually in staging via three-way baseline/worker/staging compare. Order: 175, then 179, then 178 (contract owners first, image bytes last). No silent last-writer-wins. Combined check: backup round-trip + merge counts per ticket.
- `src-tauri/src/quick_actions.rs` + `windows_execution/` (175 + 179): substantial compatible edits to the same owner (ADR-0029 — extend the owner, no second spawn/quote site). 175 owns pre_check/pre_fix validate + check-first chain + same-log headers; 179 owns files table interface + per-run staging + `<FilesDir>` quoting + cleanup. Different symbols/intent, same files. Coordinator is merge owner; staging three-way compare; combined Rust tests validate the chain + staging interaction. 179 assumes 175's run/log chain shape — pinned above, not a runtime dependency.
- `src/routes/quick-launch-window/+page.svelte` (177 + 178): 177 owns Companion toolbar/lifetime/child-creation + Back/Forward + reload=saved regions; 178 owns Clips tab/rows/thumbnail/details/copy regions. Disjoint intents; coordinator merges regions in staging. 177 also appends ADR-0022 Amendment (single writer — no conflict).
- `src/lib/api.ts` + `src/lib/types.ts` (175 + 178 + 179): each appends different exported symbols (pre-action commands, files commands, clip-image commands). Coordinator merges disjoint symbol blocks.
- `src/routes/logs/+page.svelte` (174 only): single writer, copied verbatim. No overlap.
- Shared owners/tokens/constants/version (`constants/window.rs`, tokens, components, ownership inventory/gate): no changes allowed by any worker without coordinator approval. 177/178 must scan `constants/window.rs` before any dimension claim and reuse tokens/components; deviations go in the return for review.

## Integration order

1. Collect returns, freeze artifacts. 2. Reconcile in staging against exact baseline: apply only changed paths per return (174 → 175 → 179 → 178 → 177, or arrival order for ready units; 175 before 179 where they touch the same owner). Three-way compare for every overlapping file; preserve both compatible changes. 3. Apply candidate to `C:\Sprout` path-by-path with pre-apply recovery snapshot outside tree. 4. Validate combination in `C:\Sprout` (toolchain checks + ticket tests + `node tools/ownership-gate.mjs`). 5. Mark ACs only for applied+validated tickets, each in its own issue file. 6. Publish per completed unit with `-Up` twice (expect 0 copied). 7. 176/180 start later from the validated integrated baseline (contract handoff from 175/179).

## Conflict ledger

- `src-tauri/src/quick_actions.rs` (175 + 179): 175's pre_check/pre_fix shape/chain/tests kept; 179's files block inserted after `move_quick_action`; `append_action` merged to 179's `Result<i64>` return with 175's 12-column params; both test sets kept (shared `conn`/`input` helpers — 175's `input` carries the new fields). Validated by `cargo test` (quick_actions scope green). Coordinator repair: bind-`rows`-first in the two list fns (tail-`?` borrow error in 179's code) + realistic legacy fixture (predated even `cwd`).
- `src-tauri/src/db.rs` (175 + 178 + 179): schema gains pre_check/pre_fix columns AND `clip_images` AND `quick_action_files` tables; migrate calls all three ensures (order: pre_action → show_in_dock → clip_images → item_group → files); three ensure fns coexist; 175's 2 literals kept; 178's clip_images migrate test added. Validated by `cargo test` (db scope green).
- `src-tauri/src/lib.rs` (175 + 178 + 179): 175's Outcome/check-first chain kept; 179's staging woven in after a passing check (blocked runs stage nothing) + files commands + B64 import + registrations; 178's copy_clip guard + image-clip section + registrations kept (178's `start_tracked_run` verified byte-identical to baseline — no merge needed there). Validated by `cargo test` + gate.
- `src-tauri/src/backup.rs` (175 + 178 + 179): 179's files machinery kept; 178's clip-merge split applied (B64 alias); 175's 3 pre-action tests + 178's fixture/3 image tests appended; all `ClipInput` literals gained `image: None`, `action()` gained pre_check/pre_fix. Validated by `cargo test` (backup scope green).
- `src/routes/quick-launch-window/+page.svelte` (177 + 178): 177's Companion trail/reload rework kept; 178's 4 clip hunks applied (imports, image-routed copy + clipName, rows, token-only CSS). Validated by `npm.cmd run check` 0/0 + `npm.cmd test` 212 pass.
- `src-tauri/src/groups.rs` (178 + 175 knock-on): 178's `image: None` kept; 175's `pre_check: None, pre_fix: None` added by coordinator. `src-tauri/src/ai_assist.rs`: 175's knock-on added by coordinator. Validated by `cargo test` + `cargo check` 0/0.
- Coordinator repairs on the candidate (all mirrored to staging; recovery snapshot at `...\batch-174-175-177-178-179-20260911\recovery-pre-candidate-01`): borrow-order fix (2 fns), unused re-export trim (zero-warning check), legacy-fixture realism, TextInput/InfoTip imports (check clean). No silent last-writer-wins anywhere; every overlapping file got a three-way baseline/worker/staging review.
- 177 AC1 deviation (reported, not absorbed): switch-trail ships; native in-pane traversal needs a follow-up Rust seam ticket. 171 NOT claimed resolved.

## Validation record (candidate-01, tested inputs frozen in C:\Sprout)

- `cargo check` (src-tauri): exit 0, 0 errors, 0 warnings. Log: `...\batch-174-175-177-178-179-20260911\cargo_check3.txt`.
- `cargo test` (src-tauri): 585 passed, 0 failed, 3 ignored. Log: `...\cargo_test_full2.txt` (first run: 584/1 — the legacy-fixture failure, repaired, re-run green).
- `npm.cmd run check`: 0 errors, 0 warnings (after import repair; first run: 5 errors, all missing imports). Logs: `npm_check.txt`, `npm_check2.txt`.
- `npm.cmd test -- --run`: 18 files, 212 passed. Log: `npm_test.txt`.
- `node tools/ownership-gate.mjs`: pass (62 owned references). Ran on the final tree.
- Pending (honest, not blocking publication of the integrated unit): manual matrices for 175 (Tauri run chain), 177 (dock matrix), 178 (dock/a11y), 179 (quoting matrix) + 177-AC1 native-seam follow-up + 178-AC2 manual. Test-only `into_path` deprecation warnings (78, pre-existing pattern) left untouched — `cargo check` (the enforced gate) is clean.

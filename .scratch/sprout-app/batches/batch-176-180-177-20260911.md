# Batch batch-176-180-177-20260911 — parallel 176 + 180 + 177

Coordinator: single session in `C:\Sprout`. Core `-Down` ran 2026-09-11 as first action (0 copied, snapshot 472 files). ADR index read; relevant ADRs read in full: 0022 (Companion, incl. 2026-09-11 Back/Forward amendment), 0029 (ownership), plus 0017/0014/0026 via prior batch baseline. Source baseline: `.sync-state.json` taken `2026-09-11T08:52:24.6222755-07:00`, copied immutable to `C:\Users\admin\AppData\Local\Temp\opencode\batch-176-180-177-20260911\baseline-hashes.json` (outside synced tree).

Spec owner: 173. All three tickets are wave-2/independent per spec table: 176 after 175 (contract present in tree: `pre_check`/`pre_fix` Option<String>, `run_quick_action` -> `QuickActionRunOutcome::Started|CheckBlocked`, `run_quick_action_fix`, PRE_ACTION_TIMEOUT 10s, same-log headers), 180 after 179 (contract present: `quick_action_files` FK CASCADE, `FILES_DIR_PLACEHOLDER="<FilesDir>"` in `windows_execution/files.rs`, stage+quote+expand in owner, inert-without-placeholder, `list/attach/remove_quick_action_file` commands), 177 independent (assumes 170 lifecycle; 171 investigation-only gates nothing; rework from switch-trail to native history). Prior batch `batch-174-175-177-178-179-20260911` validated 175/179 and shipped 177-trail (AC1 left open — this batch closes it natively).

## Ticket table

| Ticket | Dependencies | Baseline | Workspace | Claimed paths / owners | Shared contract / integration owner | Actual changes | Validation assignment / results | State |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 176 Pre-action frontend (worker) | 175 stored shape + outcome contract (present, pinned below); 172 dialog template; 167 copy rules | sync 2026-09-11T08:52:24, 472 files | `...\batch-176-180-177-20260911\worker-176` | EDIT `src/lib/components/QuickActionFormDialog.svelte` (Pre-action section only: pre_check/pre_fix fields + [Check] + hints); EDIT `src/routes/quick-actions/+page.svelte` (run-warn wiring only); EDIT `src/lib/api.ts`/`types.ts` (pre-action seam symbols only: input fields, outcome/payload types, run/fix/check wrappers) | Pinned: `QuickActionInput.pre_check/pre_fix: string\|null` trimmed empty→null (mirrors backend empty→None); fix-without-check blocked plain error; `run_quick_action(id): Promise<{outcome:'started'}\|{outcome:'check_blocked', passed, output, timed_out, exit_code, duration_ms, has_fix, log_path}>`; `runQuickActionFix(id, log_path?): Promise<{exit_code, timed_out, output, duration_ms}>`; `[Check]` = `testQuickAction(shell, check, cwd)` timeboxed, never runs main/fix; warn dialog = `Dialog`+`Notice`+`TestResult`, one primary, focus/keyboard/SR; empty section persists nothing. Integration owner: coordinator merges dialog file with 180 | RETURNED frozen: dialog 48531 B (6 files incl. NEW preActionDialog.close.test.ts 5 cases + ticket ACs/done notes); CONTRACT GAP reported: 175 ships no bypass seam so [Run anyway] = fresh Run (re-checks); Quick Launch caller ignores new outcome (silent no-op on blocked check there — follow-up). Checks queued, none run locally | returned |
| 180 Files frontend + editor (worker) | 179 placeholder + file contract (present, pinned below); 172 dialog template; 167 copy rules | same baseline | `...\batch-176-180-177-20260911\worker-180` | EDIT `src/lib/components/QuickActionFormDialog.svelte` (Files UI + command editor only: attach/file-list/remove/size, PS/CMD coloring, `<` autocomplete, Insert affordance, unused/missing hints); EDIT `src/lib/api.ts`/`types.ts` (files seam symbols only: file list/attach/remove wrappers + types) | Pinned: `FILES_DIR_PLACEHOLDER="<FilesDir>"` literal reused, never redeclared; basename-only CI-unique, 5MB/file 20MB/action caps surfaced inline plain errors (backend enforces); `<FilesDir>` + after-`<FilesDir>\` filename autocomplete, correctly quoted insert; coloring = PS/CMD token classes only (comments/strings/commands distinct), never breaks editing (paste/undo/IME); hints exactly in unused/missing states. No Monaco, no new shell, no AI. Integration owner: coordinator merges dialog file with 176 | RETURNED frozen: dialog 1843 lines + NEW quickActionEditor.ts (315 lines, hand-rolled scanner, zero deps) + NEW quickActionEditor.test.ts (223 lines) + ticket ACs/done notes; deviations: extra new lib file (needs ack), Esc keeps dialog close, CMD `<` false-positive accepted. Add-mode create-then-attach failure leaves created action (follow-up suggested). Checks queued | returned |
| 177 Companion native history (coordinator-owned) | None — independent. Assumes 170 lifecycle; 171 gates nothing | same baseline | `...\batch-176-180-177-20260911\coord-177` | NEW `src-tauri/src/companion_history.rs` (owner recorded; label reused from audio owner, never redeclared; no second invocation site) + `src-tauri/src/lib.rs` (mod + 4 commands + registrations); EDIT `src/routes/quick-launch-window/+page.svelte` (toolbar rebind: native state, Rust steps, event listen, hook on creation, refresh on switch-refresh/retry; trail kept for preview fallback); EDIT `src/lib/api.ts`/`types.ts` (history seam); EDIT `docs/adr/0022-companion-single-isolated-site.md` (dated scope-correction note appended, originals untouched); EDIT `src/lib/companionPane.test.ts` (native contract + 2 trail tests re-scoped + failure/Back-then-reload pinned) | Pinned: Back/Forward = live native child history via `webview2-com 0.38` `GoBack`/`GoForward` + `CanGoBack`/`CanGoForward` query + `NavigationCompleted`/`HistoryChanged` frontend events (pattern of `companion_audio.rs` `with_webview`); picker stays sole switch surface; `trailApplyVisit` retires on native path (kept for iframe preview fallback only); reload/retry = saved address; no popup/new-window interception, no routing-freeze fix, no bridge, no relaxed isolation. UI-heavy rule binding (0004:1/4/5, 0006, 0008-n/a) | IMPLEMENTED in assigned copy (frozen for integration): 4 Rust unit tests; 5 new frontend contract tests. Checks queued for candidate validation | returned |

Staging: `C:\Users\admin\AppData\Local\Temp\opencode\batch-176-180-177-20260911\staging` (reconciliation only; checks run in `C:\Sprout` per single heavy slot).

## Overlap plan (before dispatch)

- `src/lib/components/QuickActionFormDialog.svelte` (176 + 180): substantial compatible edits to the same file, different symbols/intent. 176 owns Pre-action Advanced-collapsed section (`pre_check`/`pre_fix` states, validation, [Check] wiring, warn-dialog trigger). 180 owns Files section (attach/list/remove/size) + command-editor layer (coloring, `<` autocomplete, Insert, hints). Both may extend `api.ts`/`types.ts` but disjoint symbol blocks (pre-action vs files). Coordinator is merge owner; staging three-way compare (baseline/worker-176/worker-180); no silent last-writer-wins. Combined checks: `npm.cmd run check` + dialog interaction matrix + guidelines review on touched files. Workers MUST NOT reformat unrelated dialog regions; MUST stay inside allow-list; MUST report any needed shared edit before writing outside it.
- `src/lib/api.ts` + `src/lib/types.ts` (176 + 180 + 177): three writers append different exported symbols (pre-action vs files vs companion-history). Coordinator merges disjoint blocks textually in staging.
- `src/routes/quick-actions/+page.svelte` (176 only) + `src/routes/quick-launch-window/+page.svelte` (177 only): single writers each, copied verbatim barring review. No overlap.
- `src-tauri/src/lib.rs` (177 only in this batch): single writer. 175/179 regions untouched.
- Shared owners/tokens/constants/version (`constants/window.rs`, tokens.css, components/, ownership inventory/gate): no changes without coordinator approval. 176/180 reuse tokens/components only (0005 rules 1/5/6); 177 scans `constants/window.rs` before any dimension claim, toolbar fits 340px dock. Deviations go in return for review.
- ADR-0022 (177 only): append dated note extending 2026-09-11 section (native replaces switch-trail; picker owns switching); original text + 2026-09-11 section untouched. No other ticket touches docs/adr or docs/research (176/180 cite rules; if no rule fits they record new note under `docs/research/` in their own copy for coordinator review — coordinator publishes).

## Integration order

1. Collect returns, freeze artifacts (baseline/result hashes, changed paths, AC evidence, queued checks). 2. Reconcile in staging against exact baseline: apply only changed paths per return (177 → 176 → 180, or arrival order; 176+180 dialog file via three-way merge under coordinator). 3. Apply candidate to `C:\Sprout` path-by-path with pre-apply recovery snapshot outside tree. 4. Validate combination in `C:\Sprout` (toolchain checks + ticket tests + gate). 5. Mark ACs only for applied+validated tickets, each in its own issue file. 6. Publish per completed unit with `-Up` twice (expect 0 copied). 7. Failed/timeout: retain artifact, exclude incomplete work + dependents, continue independents. Never apply a still-changing copy.

## Conflict ledger

- `src/lib/components/QuickActionFormDialog.svelte` (176 + 180): three-way baseline/worker-176/worker-180 compare; both changes preserved, no last-writer-wins. Staging starts from 176's version (48531 B) then ports 180's regions: api+quickActionEditor imports, submit create-branch attach loop (pre_check/pre_fix payloads kept), AttachedFileRow + file/editor state + all file functions before `</script>`, open-effect file/editor reset, command overlay + suggestions + hints + Files block replacing the plain textarea, cmdwrap/tok/suggest/files CSS before the preaction block. Shared Save/Cancel guard keeps 176's `saving || testing || checking` (180 left it untouched; files buttons carry their own `filesBusy`). Merged result 2038 lines. Combined check: `npm.cmd run check` + dialog tests + guidelines review on touched files.
- `src/lib/api.ts` (176 + 180 + 177): three disjoint blocks merged in staging from 176's version: 180's Action-files section (caps consts + list/attach/remove/format + `QuickActionFileMeta` import) after `testQuickAction`; 177's history section (state query + go-back/forward + hook + event const) after `setCompanionMuted`. No overlapping lines.
- `src/lib/types.ts` (176 + 180 + 177): three disjoint blocks merged in staging from 176's version: 180's `QuickActionFileMeta` after `QuickActionRunOutcome`; 177's `CompanionHistoryState` after `CompanionAudioState`. No overlapping lines.
- Single-writer paths copied verbatim to staging: 176's quick-actions page + preActionDialog test + ticket; 180's quickActionEditor.ts + test + ticket; 177's companion_history.rs + lib.rs + quick-launch-window page + companionPane.test.ts + ADR note + ticket.
- 176 CONTRACT GAP (reported, not absorbed): 175 ships no bypass seam so [Run anyway] = fresh Run (re-checks); Quick Launch caller ignores the new outcome (silent no-op on blocked check there) — follow-ups, not blockers for this unit.
- 180 deviations accepted into staging: NEW `src/lib/quickActionEditor.ts` (dialog stays thin; deletion test holds); Esc keeps dialog close; CMD `<` false-positive tradeoff noted. Add-mode create-then-attach failure shape noted as follow-up.

## Validation record

- `npm.cmd run check`: 0 errors, 0 warnings (after coordinator repairs below; first run: 4 errors, all in 180's new test file).
- Focused `npm.cmd test -- --run src/lib/quickActionEditor.test.ts src/lib/preActionDialog.close.test.ts src/lib/companionPane.test.ts`: 3 files, 84 passed at the quoted-hint stage; full suite re-ran green after the slash-separator fix (see below).
- Full `npm.cmd test -- --run`: 20 files, 244 passed.
- `cargo check` (src-tauri): 0 errors, 0 warnings (after removing 3 unused `windows_core::Interface` imports in the new history module; first run: 0 errors, 3 warnings).
- `cargo test` (src-tauri): 592 run — 589 passed, 0 failed, 3 ignored (incl. 4 new `companion_history` unit tests; the 77 `into_path` deprecation warnings are pre-existing). First attempt BLOCKED on disk (ENOSPC linking with 2.8 GB free); freed ~5 GB by removing `target\debug\incremental` only (regenerable cache — `deps`/`build` dependency artifacts kept, no cold rebuild), re-ran green.
- `node tools/ownership-gate.mjs`: pass (62 owned references, all in their owner files).
- Manual matrices (175 run chain, 176 dialog, 177 dock, 179 quoting, 180 files/editor): pending a full runtime pass. First live evidence 2026-09-11 (user-run): `notepad <FilesDir>\opencode.json` with 1 file attached staged, spawned (pid 16108), exited 0 — stored command + attached count logged, staged path never persisted.

## Coordinator repairs (mechanical; mirrored to staging; worker artifacts frozen)

- 180 `quickActionEditor.test.ts`: TS-invalid PowerShell corpus string (`'don''t'` — PS escaping, not TS) rewritten as a double-quoted literal; wrong `../package.json` (resolves to `src/package.json`) corrected to `../../package.json`. Neither changes the test's intent (roundtrip corpus + zero-dep pin).
- 177 `companion_history.rs`: 3 unused `windows_core::Interface` imports removed (no `.cast()` in this owner, unlike the audio owner) for the zero-warning gate.
- 180 user-feedback refinements (2026-09-11, reported against candidate-01, implemented by coordinator as small UI-only deltas on the frozen return):
  - Insert is a real button now (`variant="secondary"`, plain "Insert" label per report) and waits for files (`disabled={filesBusy || fileRows.length === 0}`) — inserting with nothing attached was a dead click into the "missing" hint.
  - `<`-autocomplete offers `<FilesDir>\<name>` per attached file matching the typed partial (bare `<` offers all), so two files are directly pickable — previously only the bare folder was offered and a file needed the full `<FilesDir>\` prefix first. After-separator filename offers unchanged. Suggestion detail reads the size back through the filename so `<FilesDir>`-path rows stay distinguishable (`suggestDetail` helper).
  - Caps stated in the Files InfoTip (5 MB/file, 20 MB/action, oversized picks skipped inline); over-cap picks were and stay inline `role=alert` skips — nothing thrown, dialog stays open, backend revalidates on save. `<` with no files still offers the bare folder (placeholder-first authoring is the designed "missing"-hint flow; per-file paths are already suppressed when empty).
  - Tokenizer takes `filenames` (default `[]`, old calls unaffected) and extends the placeholder token over `\<attached-name>` (case-insensitive, user's casing kept), so a file reference highlights as one unit; dialog passes `fileNames`.
  - Double-edge under the command box removed: chrome (bg/border/radius/focus ring) lives once on `.cmdwrap` with `:focus-within`; the highlight copy and textarea paint text only (`border: 0`, transparent bg) and the textarea is `display: block` — the inline baseline gap under it plus two overlapping borders read as a second seam at the bottom edge. Focus indication preserved via the wrap ring.
  - Self-quoted placeholder flagged: `filesHint` gains `"quoted"` (author wrapped `<FilesDir>` in `'`/`"` while files are attached — the owner already quotes per shell, so that double-quotes and breaks at run; verified against `windows_execution/files.rs` expansion). No-files-quoted still reads "missing" as the single next step. Run-time behavior unchanged (strict owner quoting stays a 179-contract matter).
  - Forward slash stages the same folder: filename offers and attached-name highlighting trigger after `<FilesDir>/` as well as `<FilesDir>\` (user typed `/` and got nothing — the trigger was backslash-only). Tag-branch path items still insert with `\`.
  - Tests: new completion/path/splice cases + Insert-button contract test + single-chrome test + attached-filename highlight test (84 pass in the focused run).

## Pre-existing snapshot anomaly (not a mid-session change)

- `docs/adr/0022-companion-single-isolated-site.md`: `.sync-state.json` (taken 2026-09-11T08:52:24) records `1E35…`, but the file on disk hashes `7A54…` — untouched since 6:57 AM (prior batch's amendment publication) and byte-identical to this session's baseline copies (`worker-176` copy matches disk). No one changed it mid-session; the snapshot entry predates the publication write. Pre-apply bytes preserved in `recovery-pre-candidate-01\0022.md`; the candidate overwrites it with the reviewed amendment extension. If `-Up` reports `SHARE-NEWER` here, resolve explicitly per `working-copy.md`.
